{
  "description": "memory-system hooks: PreToolUse injects project + global memory once per session (session-based dedup + subagent-boundary re-injection + mtime invalidation). Stop spawns a detached worker that asks Claude to audit the recent turn and persist anything memo-worthy via the memory-system skill.",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "*",
        "hooks": [
          {
            "type": "command",
            "command": "node ${CLAUDE_PLUGIN_ROOT}/hooks/inject-memory.mjs",
            "timeout": 5
          }
        ]
      }
    ],
    "Stop": [
      {
        "matcher": "*",
        "hooks": [
          {
            "type": "command",
            "command": "node ${CLAUDE_PLUGIN_ROOT}/hooks/audit-memory.mjs",
            "timeout": 5
          }
        ]
      }
    ]
  }
}
